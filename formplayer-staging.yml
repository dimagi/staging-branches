# See README.md for details on how this is used

trunk: master
name: autostaging
branches:
  - riese/fix_cache_collision
submodules:
  libs/commcare:
    trunk: master
    branches:
      - riese/cache_key
